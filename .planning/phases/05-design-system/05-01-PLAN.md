---
phase: 05-design-system
plan: 01
type: execute
---

<objective>
Extract design tokens from existing UI code and create a centralized design constants file.

Purpose: Consolidate scattered inline styles into reusable constants for consistency and maintainability.
Output: `extension/modules/design.js` with all design tokens (colors, typography, spacing, shadows).
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior plan context:
@.planning/phases/04-code-quality/04-03-SUMMARY.md

# Key files:
@extension/modules/ui.js
@extension/modules/utils.js

**Tech stack available:** Chrome Extension MV3, ES modules
**Established patterns:**
- Dynamic imports with chrome.runtime.getURL() (04-03)
- modules/*.js structure for extension code (04-03)
- getCefrColor() in utils.js for CEFR level colors

**Current design patterns (extracted from ui.js):**
- Primary gradient: `linear-gradient(135deg, #667eea 0%, #764ba2 100%)`
- Success color: `#4CAF50`
- Error color: `#f44336`
- Warning/orange: `#FF9800`, `#F57C00`
- Typography: Cinzel, Playfair Display, Georgia (serif fallback)
- Box shadows: `rgba(0,0,0,0.3)` variants
- Border radius: 6px, 8px, 12px, 16px
- CEFR colors: A1=#4CAF50, A2=#8BC34A, B1=#FFC107, B2=#FF9800, C1=#FF5722, C2=#9C27B0
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create design tokens module</name>
  <files>extension/modules/design.js</files>
  <action>
Create a centralized design tokens file with all design constants extracted from ui.js:

```javascript
// Design tokens for Read & Learn extension
// Centralized design system constants

export const COLORS = {
  // Primary brand gradient
  primary: {
    gradient: 'linear-gradient(135deg, #667eea 0%, #764ba2 100%)',
    start: '#667eea',
    end: '#764ba2',
    shadow: 'rgba(102,126,234,0.4)'
  },

  // Status colors
  success: '#4CAF50',
  error: '#f44336',
  warning: '#FF9800',
  info: '#2196F3',

  // CEFR level colors (progression from green to purple)
  cefr: {
    A1: '#4CAF50',
    A2: '#8BC34A',
    B1: '#FFC107',
    B2: '#FF9800',
    C1: '#FF5722',
    C2: '#9C27B0'
  },

  // UI element colors
  background: {
    primary: '#ffffff',
    secondary: '#f5f5f5',
    hover: '#e3f2fd'
  },

  // Text colors
  text: {
    primary: '#333333',
    secondary: '#666666',
    muted: '#888888',
    inverse: '#ffffff'
  },

  // Border colors
  border: {
    light: '#e0e0e0',
    default: '#ddd'
  },

  // Feature-specific
  vocabulary: {
    background: '#e8eaf6',
    text: '#3f51b5'
  },
  grammar: {
    background: '#fff3e0',
    text: '#f57c00'
  },
  highlight: {
    background: 'linear-gradient(135deg, #FFF3E0 0%, #FFE0B2 100%)',
    border: '#FF9800'
  },
  delete: {
    background: '#ffebee',
    text: '#c62828'
  }
};

export const TYPOGRAPHY = {
  // Font families
  fontFamily: {
    display: '"Cinzel", "Playfair Display", "Bodoni MT", "Didot", "Georgia", serif',
    body: "'SF Pro Display', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif"
  },

  // Font sizes
  fontSize: {
    xs: '11px',
    sm: '12px',
    base: '13px',
    md: '14px',
    lg: '16px',
    xl: '18px',
    '2xl': '20px',
    '3xl': '36px'
  },

  // Font weights
  fontWeight: {
    normal: '400',
    medium: '600',
    bold: '700',
    black: '900'
  },

  // Line heights
  lineHeight: {
    tight: '1.1',
    normal: '1.6'
  },

  // Letter spacing
  letterSpacing: {
    tight: '1px',
    wide: '2px'
  }
};

export const SPACING = {
  // Padding/margin scale
  xs: '4px',
  sm: '6px',
  md: '8px',
  lg: '12px',
  xl: '16px',
  '2xl': '20px',
  '3xl': '24px',
  '4xl': '40px'
};

export const BORDERS = {
  // Border radius
  radius: {
    sm: '3px',
    md: '6px',
    lg: '8px',
    xl: '12px',
    pill: '16px'
  },

  // Border widths
  width: {
    thin: '1px',
    medium: '2px',
    thick: '4px'
  }
};

export const SHADOWS = {
  // Box shadows
  sm: '0 2px 8px rgba(0,0,0,0.08)',
  md: '0 2px 10px rgba(0,0,0,0.2)',
  lg: '0 4px 20px rgba(0,0,0,0.2)',
  xl: '0 8px 32px rgba(0,0,0,0.12), 0 2px 8px rgba(0,0,0,0.08)',

  // Button shadows
  button: '-2px 4px 20px rgba(0,0,0,0.3)',
  buttonHover: '-4px 6px 28px rgba(0,0,0,0.4)',

  // Text shadows
  text: '0 2px 4px rgba(0,0,0,0.3)'
};

export const TRANSITIONS = {
  fast: 'all 0.2s ease',
  medium: 'all 0.25s cubic-bezier(0.4, 0, 0.2, 1)',
  slow: 'all 0.3s ease'
};

export const Z_INDEX = {
  banner: 999999,
  button: 999999,
  menu: 999998,
  tooltip: 999999,
  popup: 999999
};
```
  </action>
  <verify>File exists and exports all design tokens</verify>
  <done>design.js created with COLORS, TYPOGRAPHY, SPACING, BORDERS, SHADOWS, TRANSITIONS, Z_INDEX exports</done>
</task>

<task type="auto">
  <name>Task 2: Update utils.js to use design tokens</name>
  <files>extension/modules/utils.js</files>
  <action>
Update getCefrColor() to import from design.js instead of hardcoding colors:

1. Add import at top of utils.js:
```javascript
import { COLORS } from './design.js';
```

2. Update getCefrColor function:
```javascript
export function getCefrColor(level) {
  return COLORS.cefr[level] || COLORS.primary.start;
}
```

This maintains backward compatibility while centralizing the source of truth.
  </action>
  <verify>grep -n "import.*design" extension/modules/utils.js shows import line</verify>
  <done>getCefrColor uses COLORS.cefr from design.js</done>
</task>

<task type="auto">
  <name>Task 3: Update manifest.json for design.js</name>
  <files>extension/manifest.json</files>
  <action>
The web_accessible_resources already includes `modules/*.js` pattern, so design.js will be automatically accessible. Verify this is the case by checking manifest.json.

If somehow the pattern is more restrictive, update to ensure design.js is included.
  </action>
  <verify>grep "modules" extension/manifest.json confirms modules/*.js pattern</verify>
  <done>design.js accessible as web resource (covered by existing wildcard)</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] extension/modules/design.js exists with all design tokens
- [ ] utils.js imports and uses COLORS.cefr
- [ ] No syntax errors in design.js (can be imported)
- [ ] getCefrColor still returns correct colors
</verification>

<success_criteria>
- design.js created with comprehensive design tokens
- utils.js updated to use centralized colors
- All existing functionality preserved
- Design tokens ready for ui.js refactoring in next plan
</success_criteria>

<output>
After completion, create `.planning/phases/05-design-system/05-01-SUMMARY.md`:

# Phase 5 Plan 01: Design Tokens Extraction Summary

**[Substantive one-liner about what shipped]**

## Accomplishments
- [Key outcomes]

## Files Created/Modified
- [Files with descriptions]

## Decisions Made
- [Any runtime decisions]

## Issues Encountered
- [Problems and resolutions]

## Next Step
- Ready for 05-02: Design System Documentation
</output>
