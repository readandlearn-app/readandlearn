# ========================================
# Read & Learn - Environment Configuration
# ========================================

# Server Configuration
PORT=3000

# Rate Limiting
# Window duration in milliseconds (default: 60000 = 1 minute)
RATE_LIMIT_WINDOW_MS=60000
# Maximum requests per window (default: 30)
# Normal usage is ~5-10 requests per article analysis. 30 allows bursts while preventing abuse.
RATE_LIMIT_MAX_REQUESTS=30

# CORS Configuration
# Comma-separated list of allowed origins
# Supports patterns: chrome-extension://* (any extension), http://localhost:* (any localhost port)
# Leave empty or omit to use defaults: chrome-extension://*,http://localhost:*,http://127.0.0.1:*
ALLOWED_ORIGINS=chrome-extension://*,http://localhost:*,http://127.0.0.1:*

# AI API (REQUIRED)
# Get your API key from your AI provider
CLAUDE_API_KEY=your-ai-api-key-here

# PostgreSQL Configuration
# These defaults work with docker-compose setup
DB_HOST=localhost
DB_PORT=5432
DB_NAME=readandlearn
DB_USER=readandlearn
DB_PASSWORD=dev_password_123

# Feature Flags
# Enable caching to reduce API costs (recommended: true)
ENABLE_CACHING=true

# Enable usage analytics (optional: true/false)
ENABLE_ANALYTICS=true

# Performance Tuning
# Maximum words to analyze per article (reduces token usage)
# Recommended: 800 (good balance of accuracy and cost)
MAX_TEXT_WORDS=800

# Use smart text sampling (recommended: true)
USE_SMART_SAMPLING=true

# Optional: Voyage AI API Key (for embeddings)
# If not provided, will use local embeddings via @xenova/transformers
# VOYAGE_API_KEY=your_voyage_api_key_here
